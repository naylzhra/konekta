[project]
name = "konekta-shared-utils"
version = "0.1.0"
description = "Shared utility functions for KONEKTA Python services (e.g. geo helpers)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
