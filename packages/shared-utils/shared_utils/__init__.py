# TODO: define shared utility functions (e.g. haversine distance, geo
# helpers, common env/config loading) used across the Python services.
#
# Not yet installed into any service (would be `pip install -e
# ../../packages/shared-utils` once real utils land).
