# TODO: define shared Pydantic models (VirtualStop, RideRequest,
# FeederLocation, DemandPoint, etc.) used across gateway, pooling-service,
# stop-optimization-service, and forecasting-service.
#
# Not yet installed into any service (would be `pip install -e
# ../../packages/shared-models` once real models land, to avoid duplicating
# schema definitions across services).
